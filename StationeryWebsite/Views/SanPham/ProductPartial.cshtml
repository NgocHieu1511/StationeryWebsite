
    @using PagedList.Mvc
    @model PagedList.IPagedList<StationeryWebsite.Models.Product>

    <div class="row row-gap-4 mb-48">

        @foreach (var item in Model)
        {
            <div class="col-lg-4 col-md-6 col-sm-6 wow fadeInUp animated">

                <div class="latest-product-card">

                    <div class="image-box mb-16">

                        @if (item.is_sale)
                        {
                            <span class="sale-label subtitle fw-400 white">
                                Sale
                            </span>
                        }

                        <a href="@Url.Action("Details", "SanPham", new { id = item.product_id })"
                           class="image">

                            <img src="@Url.Content(item.image)"
                                 class="product-image"
                                 alt="@item.name">

                        </a>

                        <div class="side-icons">

                            <ul class="list-unstyled">

                                <li>
                                    <a href="#">
                                        <i class="fa-light fa-heart"></i>
                                    </a>
                                </li>

                                <li>
                                    <a href="#"
                                       class="btn"
                                       data-bs-toggle="modal"
                                       data-bs-target="#productQuickView">

                                        <i class="fa-regular fa-eye"></i>

                                    </a>
                                </li>

                            </ul>

                        </div>

                        <a href="@Url.Action("AddToCart", "GioHang", new { id = item.product_id })"
                           class="cus-btn">

                            <span class="btn-text">
                                Add Cart
                            </span>

                            <span>
                                Go to Cart
                            </span>

                        </a>

                    </div>

                    <div class="product-desc">

                        <div>

                            <a href="@Url.Action("Details", "SanPham", new { id = item.product_id })"
                               class="product-title h6 fw-700 mb-8">

                                @item.name

                            </a>

                            <div class="d-flex align-items-center gap-8 justify-content-center mb-8">

                                <div class="rating-stars">
                                    ★★★★★
                                </div>

                                <p class="fw-500">
                                    (5)
                                </p>

                            </div>

                            <h5 class="medium-black fw-700">

                                @String.Format("{0:N0} đ", item.price)

                            </h5>

                        </div>

                    </div>

                </div>

            </div>
        }

    </div>

    <!-- PAGINATION -->

    <div class="pagination mt-48">

        @Html.PagedListPager(
            Model,
            page => Url.Action("SanPham", "SanPham", new { page }),
            new PagedListRenderOptions
            {
                LiElementClasses = new[] { "page-item" },
                UlElementClasses = new[] { "pagination-list" }
            }
        )

    </div>
